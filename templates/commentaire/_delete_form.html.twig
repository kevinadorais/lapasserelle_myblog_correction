<form method="post" action="{{ path('myblog_commentaire_delete', {article: commentaire.article.id, 'commentaire': commentaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commentaire.id) }}">
    <button class="btn">Supprimer</button>
</form>
